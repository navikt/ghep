nada:
  commits: "#commits"
  issues: "#issues"
  pulls: "#pulls"
  workflows: "#workflows"
  releases: "#releases"
  security: "#security"
  config:
    ignoreRepositories:
      - repoA
    silenceDependabot: "always"
    externalContributorsChannel: "#external"
    security:
      severityFilter: "high"
    pingSlackUsers: true
    workflows:
      ignoreBots: true
      branches:
        - main
      workflows:
        - deploy
      repositories:
        - my-little-repo
