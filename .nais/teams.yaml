apiVersion: v1
kind: ConfigMap
metadata:
  name: ghep-teams
  namespace: the-g-team
data:
  teams.yaml: |
    nada:
      channels:
        commits: "#nada-commits"
        issues: "#nada-issues"
        pulls: "#nada-pull-requests"
